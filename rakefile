desc "Updates GitHub."
# Usage: rake ship "Commit message."
task :ship do
  message = ARGV.last
  task message.to_sym do ; end
  system "git add -A"
  system "git commit -am '#{message}'"
  system "git pull origin experiments"
  system "git push origin experiments"
  puts "Github update attempt complete."
end
